<h1>Trips</h1>
<ul>
  <% @trips.each do |trip| %>
    <li><%= link_to trip.title, trip %> <%= trip.id %>, <%= trip.description %></li>
  <% end %>

  <% if @trips.empty? %>
    <p>Aucun voyage</p>
  <% end %>
</ul>
