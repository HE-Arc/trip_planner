<% content_for :title, "My Trips" %>

<h1>My Trips</h1>
<ul>
  <% @trips.each do |trip| %>
    <li><%= link_to trip.title, trip %> <%= trip.id %>, <%= trip.description %></li>
  <% end %>

  <% if @trips.empty? %>
    <p>Aucun voyage</p>
  <% end %>
</ul>
